SYS_DATABASE_URL="postgresql://braaaaam:password@postgres:5432/sys"
ORG_DATABASE_URL="postgresql://braaaaam:password@postgres:5432/org"

# Email Configuration
SMTP_HOST=mail.domain.com
SMTP_PORT=587
SMTP_USER=your-username
SMTP_PASS=your-password
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=OrgName

# Email Scheduler Configuration
# Runs every 15 minutes
EMAIL_JOB_CRON=*/15 * * * *
CRON_TIMEZONE=America/New_York
DEFAULT_ORG_CLOSE_TIME=17:00
EMAIL_BATCH_SIZE=50

# Application URL (used in password reset emails)
APP_URL=http://localhost:3000