import { Request, Response } from "express";

// TODO: For future Controllers, make stubs without logic. Just return res.status(500) for now...
export const listOrganizations = (req: Request, res: Response): Response => {
    return res.status(500).send();
};

export const createOrganization = (req: Request, res: Response): Response => {
    return res.status(500).send();
};

export const getOrganization = (req: Request, res: Response): Response => {
    return res.status(500).send();
};

export const updateOrganization = (req: Request, res: Response): Response => {
    return res.status(500).send();
};
